import React from "react";
import Header from "../header/header";
import styles from "./activity.module.css";

const Activity = ({ authService }) => <Header authService={authService} />;

export default Activity;
