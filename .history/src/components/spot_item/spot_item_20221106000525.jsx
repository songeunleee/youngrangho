import React from "react";
import styles from "./spot_item.module.css";
const SpotItem = ({ spot }) => {
  const spot = { fileURL };
  return (
    <section>
      <img src={fileURL} alt="" />
    </section>
  );
};

export default SpotItem;
